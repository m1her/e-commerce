/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["img.freepik.com", "i.pinimg.com", "mir-s3-cdn-cf.behance.net", "incrediblefeets.com"],
  },
};

module.exports = nextConfig;
